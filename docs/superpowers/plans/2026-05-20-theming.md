# 主题切换 + 调色板 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 v2fun 加明亮/暗色 mode 切换 + 6 色主题调色板,默认改为柔和 teal,持久化到 localStorage,UI 入口在 Profile 页"外观"section。

**Architecture:** CSS 变量正交分层 — `[data-theme]` 控制 bg/text/border,`[data-accent]` 控制主题色及其派生。React `ThemeProvider` 管 state 双向同步到 `<html>` 属性和 localStorage。`index.html` inline script 在 React mount 前先注水,防 FOUC。

**Tech Stack:** React 19 + TypeScript + Vite 6,CSS Modules + CSS 变量;无单元测试框架,验证手段是 `npx tsc --noEmit` + Chrome DevTools MCP 浏览器手测。

**Spec:** `docs/superpowers/specs/2026-05-20-theming-design.md`

**Commit 策略:** 用户 CLAUDE.md 规定 commit 必须显式请示,本 plan 各 task 不自动 commit,只 stage 改动并报告 diff;最后一个 task 集中请示 commit 推 prod。

---

## File Structure

**新增 4 个文件:**

- `src/hooks/useTheme.tsx` — `ThemeProvider` + `useTheme` hook + `ACCENTS` 常量
- `src/components/ThemeSettings.tsx` — 抽出的"外观"section 组件(给 Profile 用,保持 Profile.tsx 不臃肿)
- `src/components/ThemeSettings.module.css` — mode toggle + accent swatch 样式

**修改 6 个文件:**

- `index.html` — head 内插 inline 防 FOUC 脚本
- `src/index.css` — `:root` 内 token 拆 3 层:static / mode / accent;移除硬编码蓝
- `src/main.tsx` — 包 `<ThemeProvider>`
- `src/pages/Profile.tsx` — 插入 `<ThemeSettings/>` section
- `src/pages/Profile.module.css` — `.headerBg` 的硬编码蓝渐变换 token
- `src/components/Layout.module.css` / `PullToRefreshIndicator.module.css` / `pages/Login.module.css` — 硬编码蓝替换 token

---

## Task 1: Token 重构(static / mode / accent 三层)

**目标:** 把 `src/index.css` 的 `:root` 拆开,加 `[data-theme="dark|light"]` 和 `[data-accent=...]` 块。**此 task 完成后页面外观应不变**(还没装 inline script,但 `:root` 保留 fallback 值,所以默认仍是 dark + teal)— 注意 teal 不是当前的 cyan,所以视觉**会变**,这是预期(spec 已确认默认换 teal)。

**Files:**
- Modify: `src/index.css:20-73`(`:root` 块整段重构)

- [ ] **Step 1: 替换 `:root` 整段**

打开 `src/index.css`,把第 20-73 行(从 `:root {` 到对应 `}`)整段替换为:

```css
:root {
  /* === Static tokens (主题无关) === */
  --success: #34D399;
  --success-light: rgba(52, 211, 153, 0.08);
  --warning: #FBBF24;
  --danger: #F87171;
  --danger-light: rgba(248, 113, 113, 0.1);

  --accent-warm: #FF8A50;
  --accent-warm-light: rgba(255, 138, 80, 0.1);

  --radius-sm: 6px;
  --radius-md: 10px;
  --radius-lg: 14px;
  --radius-xl: 18px;

  --font-display: 'JetBrains Mono', 'SF Mono', monospace;
  --font-body: -apple-system, 'SF Pro Text', 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', sans-serif;
  --font-mono: 'JetBrains Mono', 'SF Mono', 'Fira Code', monospace;

  --tab-height: 50px;
  --header-height: 44px;
  --safe-top: env(safe-area-inset-top, 0px);
  --safe-bottom: env(safe-area-inset-bottom, 0px);
  --max-app-width: 640px;

  --sidebar-width: 220px;
  --desktop-list-width: clamp(420px, 30vw, 560px);
  --desktop-content-max: 920px;

  /* Fallback (无 data-theme/data-accent 时) — 实际生效靠 inline script 始终注水 */
  color-scheme: dark;
}

/* === Mode 层 === */
[data-theme="dark"] {
  --bg-primary: #0B0B10;
  --bg-secondary: #13131A;
  --bg-tertiary: #1C1C26;
  --bg-accent: #1A1A28;
  --bg-elevated: #1E1E2A;

  --text-primary: #E8E8ED;
  --text-secondary: #8F8FA3;
  --text-tertiary: #5A5A6E;

  --border: rgba(255, 255, 255, 0.06);
  --border-light: rgba(255, 255, 255, 0.04);

  --shadow-sm: 0 1px 3px rgba(0, 0, 0, 0.4);
  --shadow-md: 0 4px 12px rgba(0, 0, 0, 0.5);
  --shadow-lg: 0 8px 32px rgba(0, 0, 0, 0.6);

  color-scheme: dark;
}

[data-theme="light"] {
  --bg-primary: #F7F7FA;
  --bg-secondary: #FFFFFF;
  --bg-tertiary: #EFEFF4;
  --bg-accent: #E8E8F0;
  --bg-elevated: #FFFFFF;

  --text-primary: #1A1A24;
  --text-secondary: #5A5A6E;
  --text-tertiary: #8F8FA3;

  --border: rgba(0, 0, 0, 0.08);
  --border-light: rgba(0, 0, 0, 0.05);

  --shadow-sm: 0 1px 3px rgba(0, 0, 0, 0.06);
  --shadow-md: 0 4px 12px rgba(0, 0, 0, 0.08);
  --shadow-lg: 0 8px 32px rgba(0, 0, 0, 0.12);

  color-scheme: light;
}

/* === Accent 层 === */
[data-accent="teal"] {
  --accent: #5FA8A0;
  --accent-hover: #4D8E87;
  --accent-light: rgba(95, 168, 160, 0.10);
  --accent-glow: rgba(95, 168, 160, 0.18);
  --border-accent: rgba(95, 168, 160, 0.18);
  --glow-accent: 0 0 20px rgba(95, 168, 160, 0.18);
}

[data-accent="indigo"] {
  --accent: #7B8FD8;
  --accent-hover: #6577C2;
  --accent-light: rgba(123, 143, 216, 0.10);
  --accent-glow: rgba(123, 143, 216, 0.18);
  --border-accent: rgba(123, 143, 216, 0.18);
  --glow-accent: 0 0 20px rgba(123, 143, 216, 0.18);
}

[data-accent="sage"] {
  --accent: #7CA982;
  --accent-hover: #65906B;
  --accent-light: rgba(124, 169, 130, 0.10);
  --accent-glow: rgba(124, 169, 130, 0.18);
  --border-accent: rgba(124, 169, 130, 0.18);
  --glow-accent: 0 0 20px rgba(124, 169, 130, 0.18);
}

[data-accent="coral"] {
  --accent: #E89B6C;
  --accent-hover: #D17F50;
  --accent-light: rgba(232, 155, 108, 0.10);
  --accent-glow: rgba(232, 155, 108, 0.18);
  --border-accent: rgba(232, 155, 108, 0.18);
  --glow-accent: 0 0 20px rgba(232, 155, 108, 0.18);
}

[data-accent="plum"] {
  --accent: #B084CC;
  --accent-hover: #946BB0;
  --accent-light: rgba(176, 132, 204, 0.10);
  --accent-glow: rgba(176, 132, 204, 0.18);
  --border-accent: rgba(176, 132, 204, 0.18);
  --glow-accent: 0 0 20px rgba(176, 132, 204, 0.18);
}

[data-accent="amber"] {
  --accent: #D4A574;
  --accent-hover: #B88B5C;
  --accent-light: rgba(212, 165, 116, 0.10);
  --accent-glow: rgba(212, 165, 116, 0.18);
  --border-accent: rgba(212, 165, 116, 0.18);
  --glow-accent: 0 0 20px rgba(212, 165, 116, 0.18);
}
```

注意:`--glow-accent` 原来是 box-shadow 值(不是 rgba 颜色),所以也按 accent 派生。`--accent-warm` 跨主题保持 `#FF8A50`(感谢爱心、ConfirmDialog 用)。

- [ ] **Step 2: TypeScript 检查**

Run: `npx tsc --noEmit`
Expected: 通过(CSS 改动不影响 TS)

- [ ] **Step 3: 报告 git diff,不 commit**

Run: `git diff src/index.css | head -80`
Expected: 看到 :root 拆分

---

## Task 2: 防 FOUC inline script

**目标:** `index.html` head 加 inline script,在 React mount 前给 `<html>` 加上 `data-theme` 和 `data-accent` 属性。完成后页面应正确显示 dark + teal 主题。

**Files:**
- Modify: `index.html:14-15`(在 `<title>` 后、`</head>` 前插入)

- [ ] **Step 1: 插入 inline script**

打开 `index.html`,在 `<title>V2Fun - V2EX Client</title>` 下一行、`</head>` 上一行插入:

```html
    <script>
      (function () {
        try {
          var t = localStorage.getItem('v2fun_theme');
          var a = localStorage.getItem('v2fun_accent');
          if (t !== 'light' && t !== 'dark') t = 'dark';
          if (['teal','indigo','sage','coral','plum','amber'].indexOf(a) === -1) a = 'teal';
          document.documentElement.setAttribute('data-theme', t);
          document.documentElement.setAttribute('data-accent', a);
        } catch (e) {
          document.documentElement.setAttribute('data-theme', 'dark');
          document.documentElement.setAttribute('data-accent', 'teal');
        }
      })();
    </script>
```

允许列表写死(`teal/indigo/sage/coral/plum/amber`),防 localStorage 被人手改成乱值导致裸样式。

- [ ] **Step 2: 启动 dev server + 浏览器验证**

Run(后台): `cd /Users/jiangxuan/Projects/v2fun && npm run dev`(后台运行,等 "ready" 字样)

然后用 chrome-devtools MCP:
1. `mcp__chrome-devtools__new_page` 打开 `http://localhost:5173` (Vite 默认端口,如不是从 dev server 输出读)
2. `mcp__chrome-devtools__evaluate_script` 跑:
   ```js
   ({
     theme: document.documentElement.getAttribute('data-theme'),
     accent: document.documentElement.getAttribute('data-accent'),
     accentColor: getComputedStyle(document.documentElement).getPropertyValue('--accent').trim(),
     bgPrimary: getComputedStyle(document.documentElement).getPropertyValue('--bg-primary').trim(),
   })
   ```

Expected:
```
{ theme: 'dark', accent: 'teal', accentColor: '#5FA8A0', bgPrimary: '#0B0B10' }
```

3. 截图 `mcp__chrome-devtools__take_screenshot` 确认页面正常渲染、主题色从青蓝变成了柔和的雾松绿

- [ ] **Step 3: 关掉 dev server,报告 diff**

杀掉后台 dev server(KillShell)。

Run: `git diff index.html`
Expected: 看到 inline script

---

## Task 3: ThemeProvider + useTheme hook

**目标:** 建 React context 管 theme/accent state,双向同步 DOM 属性和 localStorage。

**Files:**
- Create: `src/hooks/useTheme.tsx`

- [ ] **Step 1: 写 hook 文件**

新建 `src/hooks/useTheme.tsx`,内容:

```tsx
import { createContext, useContext, useState, useCallback, type ReactNode } from 'react'

export type ThemeMode = 'dark' | 'light'
export type AccentSlug = 'teal' | 'indigo' | 'sage' | 'coral' | 'plum' | 'amber'

export const ACCENTS: ReadonlyArray<{ slug: AccentSlug; name: string; color: string }> = [
  { slug: 'teal', name: '雾松绿', color: '#5FA8A0' },
  { slug: 'indigo', name: '静蓝', color: '#7B8FD8' },
  { slug: 'sage', name: '鼠尾草', color: '#7CA982' },
  { slug: 'coral', name: '珊瑚橘', color: '#E89B6C' },
  { slug: 'plum', name: '梅子紫', color: '#B084CC' },
  { slug: 'amber', name: '蜜琥珀', color: '#D4A574' },
]

const STORAGE_THEME = 'v2fun_theme'
const STORAGE_ACCENT = 'v2fun_accent'
const DEFAULT_THEME: ThemeMode = 'dark'
const DEFAULT_ACCENT: AccentSlug = 'teal'

function readTheme(): ThemeMode {
  // inline script 已写到 DOM,首选 DOM 属性(单源真理)
  const attr = typeof document !== 'undefined'
    ? document.documentElement.getAttribute('data-theme')
    : null
  if (attr === 'dark' || attr === 'light') return attr
  return DEFAULT_THEME
}

function readAccent(): AccentSlug {
  const attr = typeof document !== 'undefined'
    ? document.documentElement.getAttribute('data-accent')
    : null
  if (attr && ACCENTS.some(a => a.slug === attr)) return attr as AccentSlug
  return DEFAULT_ACCENT
}

interface ThemeContextValue {
  theme: ThemeMode
  accent: AccentSlug
  setTheme: (t: ThemeMode) => void
  setAccent: (a: AccentSlug) => void
}

const ThemeContext = createContext<ThemeContextValue | null>(null)

export function ThemeProvider({ children }: { children: ReactNode }) {
  const [theme, setThemeState] = useState<ThemeMode>(() => readTheme())
  const [accent, setAccentState] = useState<AccentSlug>(() => readAccent())

  const setTheme = useCallback((t: ThemeMode) => {
    setThemeState(t)
    document.documentElement.setAttribute('data-theme', t)
    try { localStorage.setItem(STORAGE_THEME, t) } catch { /* private mode etc. */ }
  }, [])

  const setAccent = useCallback((a: AccentSlug) => {
    setAccentState(a)
    document.documentElement.setAttribute('data-accent', a)
    try { localStorage.setItem(STORAGE_ACCENT, a) } catch { /* ignore */ }
  }, [])

  return (
    <ThemeContext.Provider value={{ theme, accent, setTheme, setAccent }}>
      {children}
    </ThemeContext.Provider>
  )
}

export function useTheme(): ThemeContextValue {
  const ctx = useContext(ThemeContext)
  if (!ctx) throw new Error('useTheme must be used within ThemeProvider')
  return ctx
}
```

- [ ] **Step 2: TypeScript 检查**

Run: `npx tsc --noEmit`
Expected: 通过

- [ ] **Step 3: 报告 diff**

Run: `git status src/hooks/useTheme.tsx && git diff --stat src/hooks/useTheme.tsx`
Expected: 新增文件

---

## Task 4: 在 main.tsx 包 ThemeProvider

**目标:** App 入口包一层,后续组件能用 `useTheme()`。

**Files:**
- Modify: `src/main.tsx`

- [ ] **Step 1: 先读现状**

Run: `cat /Users/jiangxuan/Projects/v2fun/src/main.tsx`
预期看到 `<BrowserRouter><App/></BrowserRouter>` 之类结构。

- [ ] **Step 2: 插入 `<ThemeProvider>`**

把现有 root render(类似 `<StrictMode><BrowserRouter><App/></BrowserRouter></StrictMode>`)改成:

```tsx
<StrictMode>
  <ThemeProvider>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </ThemeProvider>
</StrictMode>
```

文件顶部加 import:`import { ThemeProvider } from './hooks/useTheme'`

注意:`ThemeProvider` 包在 `BrowserRouter` 外层即可(不依赖 router,只是个 context)。

- [ ] **Step 3: TypeScript 检查 + dev server 跑一遍**

Run: `npx tsc --noEmit`
Expected: 通过

后台启 `npm run dev`,chrome-devtools MCP 打开页面跑:
```js
({ theme: document.documentElement.getAttribute('data-theme'), accent: document.documentElement.getAttribute('data-accent') })
```
Expected: 仍然 `{ theme: 'dark', accent: 'teal' }`,页面无红屏错误(`mcp__chrome-devtools__list_console_messages` 应无 React error)。

杀掉 dev server。

- [ ] **Step 4: 报告 diff**

Run: `git diff src/main.tsx`

---

## Task 5: 硬编码蓝迁移到 token

**目标:** 5 个 CSS 文件里残留的 `rgba(0, 212, 255, …)` 全部替换成 token 引用。完成后切 accent 时这些位置都会跟着变色。

**Files:**
- Modify: `src/index.css:108-109, 227`
- Modify: `src/components/Layout.module.css:80, 86, 175`
- Modify: `src/components/PullToRefreshIndicator.module.css:18, 23`
- Modify: `src/pages/Login.module.css:160`
- Modify: `src/pages/Profile.module.css:24`

- [ ] **Step 1: `src/index.css`**

第 108-109 行(`body::before` 的 radial-gradient)从:
```css
    radial-gradient(ellipse at 15% 0%, rgba(0, 212, 255, 0.04) 0%, transparent 50%),
    radial-gradient(ellipse at 85% 100%, rgba(0, 212, 255, 0.02) 0%, transparent 50%);
```
改为:
```css
    radial-gradient(ellipse at 15% 0%, var(--accent-light) 0%, transparent 50%),
    radial-gradient(ellipse at 85% 100%, var(--accent-light) 0%, transparent 50%);
```

第 227 行(`.rendered-content a` 的 text-decoration-color):
```css
  text-decoration-color: rgba(0, 212, 255, 0.3);
```
改为:
```css
  text-decoration-color: var(--accent-glow);
```

- [ ] **Step 2: `src/components/Layout.module.css`**

第 80 行:
`box-shadow: 0 0 16px rgba(0, 212, 255, 0.3);` → `box-shadow: var(--glow-accent);`

第 86 行:
`box-shadow: 0 0 8px rgba(0, 212, 255, 0.3);` → `box-shadow: var(--glow-accent);`

第 175 行:
`box-shadow: 0 0 14px rgba(0, 212, 255, 0.18);` → `box-shadow: var(--glow-accent);`

(三处都用 `--glow-accent`,如发现某处实际视觉差距太大,在该处单独 override `box-shadow` — 但优先用 token。)

- [ ] **Step 3: `src/components/PullToRefreshIndicator.module.css`**

第 18 行:
`filter: drop-shadow(0 0 4px rgba(0, 212, 255, 0.5));` → `filter: drop-shadow(0 0 4px var(--accent));`

第 23 行:
`filter: drop-shadow(0 0 6px rgba(0, 212, 255, 0.65));` → `filter: drop-shadow(0 0 6px var(--accent));`

(drop-shadow 用 `var(--accent)` 直接而不是 `--accent-glow`,因为 `--accent-glow` 是个完整 `box-shadow` 值不是颜色。)

- [ ] **Step 4: `src/pages/Login.module.css`**

第 160 行:
`text-decoration-color: rgba(0, 212, 255, 0.3);` → `text-decoration-color: var(--accent-glow);`

- [ ] **Step 5: `src/pages/Profile.module.css`**

第 24 行:
`background: linear-gradient(135deg, rgba(0, 212, 255, 0.15), rgba(0, 212, 255, 0.05));`
→
`background: linear-gradient(135deg, var(--accent-glow), var(--accent-light));`

- [ ] **Step 6: 全仓搜确认无残留**

Run: `grep -rn "212, 255\|#00D4FF\|#00B8E0" /Users/jiangxuan/Projects/v2fun/src`
Expected: 无输出(全清干净)

- [ ] **Step 7: 浏览器验证**

后台启 dev server,chrome-devtools MCP:
1. 打开 / 截图,确认底部 nav(Layout.module.css 的 box-shadow)、Login 链接下划线、Profile header gradient、PullToRefresh 等地方颜色全是雾松绿,无残留蓝
2. 在 console 跑(模拟手动切 accent):
   ```js
   document.documentElement.setAttribute('data-accent', 'coral')
   ```
3. 截图,确认上面那些位置全部变成珊瑚橘
4. 再切回 teal,确认能回去
5. console 跑:
   ```js
   document.documentElement.setAttribute('data-theme', 'light')
   ```
6. 截图,确认整页 bg 变白底深字,主题色仍是 teal,所有 bg-tertiary/border 等都跟着变(无残留深色块)

杀掉 dev server。

- [ ] **Step 8: 报告 diff**

Run: `git diff --stat src/`

---

## Task 6: ThemeSettings 组件 + 样式

**目标:** 新建 `<ThemeSettings/>` 组件给 Profile 页用,内含 mode toggle + accent 调色板,样式独立。

**Files:**
- Create: `src/components/ThemeSettings.tsx`
- Create: `src/components/ThemeSettings.module.css`

- [ ] **Step 1: 写组件**

新建 `src/components/ThemeSettings.tsx`:

```tsx
import { useTheme, ACCENTS, type AccentSlug, type ThemeMode } from '../hooks/useTheme'
import styles from './ThemeSettings.module.css'

export default function ThemeSettings() {
  const { theme, accent, setTheme, setAccent } = useTheme()
  const currentName = ACCENTS.find(a => a.slug === accent)?.name ?? ''

  return (
    <div className={styles.section}>
      <h3 className={styles.sectionTitle}>外观</h3>

      <div className={styles.row}>
        <span className={styles.rowLabel}>显示模式</span>
        <div className={styles.modeToggle} role="group" aria-label="显示模式">
          <ModeBtn current={theme} value="dark" onClick={setTheme}>
            <MoonIcon /> 暗色
          </ModeBtn>
          <ModeBtn current={theme} value="light" onClick={setTheme}>
            <SunIcon /> 明亮
          </ModeBtn>
        </div>
      </div>

      <div className={styles.row}>
        <span className={styles.rowLabel}>主题色 · <span className={styles.accentName}>{currentName}</span></span>
        <div className={styles.accentRow} role="radiogroup" aria-label="主题色">
          {ACCENTS.map(({ slug, name, color }) => (
            <button
              key={slug}
              type="button"
              role="radio"
              aria-checked={accent === slug}
              aria-label={name}
              className={styles.swatch}
              data-active={accent === slug}
              style={{ background: color }}
              onClick={() => setAccent(slug as AccentSlug)}
            >
              {accent === slug && (
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#fff" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round">
                  <polyline points="20 6 9 17 4 12" />
                </svg>
              )}
            </button>
          ))}
        </div>
      </div>
    </div>
  )
}

function ModeBtn({ current, value, onClick, children }: {
  current: ThemeMode
  value: ThemeMode
  onClick: (v: ThemeMode) => void
  children: React.ReactNode
}) {
  return (
    <button
      type="button"
      className={styles.modeBtn}
      data-active={current === value}
      aria-pressed={current === value}
      onClick={() => onClick(value)}
    >
      {children}
    </button>
  )
}

function MoonIcon() {
  return (
    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round">
      <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z" />
    </svg>
  )
}

function SunIcon() {
  return (
    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round">
      <circle cx="12" cy="12" r="4" />
      <path d="M12 2v2M12 20v2M4.93 4.93l1.41 1.41M17.66 17.66l1.41 1.41M2 12h2M20 12h2M4.93 19.07l1.41-1.41M17.66 6.34l1.41-1.41" />
    </svg>
  )
}
```

- [ ] **Step 2: 写样式**

新建 `src/components/ThemeSettings.module.css`:

```css
.section {
  background: var(--bg-secondary);
  border-radius: var(--radius-md);
  padding: 14px;
  margin-bottom: 10px;
  border: 1px solid var(--border);
}

.sectionTitle {
  font-size: 0.72rem;
  font-weight: 600;
  color: var(--text-tertiary);
  text-transform: uppercase;
  letter-spacing: 0.06em;
  margin-bottom: 12px;
  font-family: var(--font-mono);
}

.row {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.row + .row {
  margin-top: 14px;
}

.rowLabel {
  font-size: 0.82rem;
  color: var(--text-secondary);
}

.accentName {
  color: var(--accent);
  font-weight: 500;
}

/* Mode toggle (segmented) */
.modeToggle {
  display: flex;
  gap: 6px;
  background: var(--bg-tertiary);
  border-radius: var(--radius-md);
  padding: 4px;
}

.modeBtn {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 6px;
  height: 32px;
  font-size: 0.82rem;
  color: var(--text-secondary);
  border-radius: var(--radius-sm);
  transition: background 0.15s, color 0.15s;
}

.modeBtn[data-active="true"] {
  background: var(--bg-elevated);
  color: var(--accent);
  box-shadow: var(--shadow-sm);
}

.modeBtn:active {
  transform: scale(0.98);
}

/* Accent swatches */
.accentRow {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
}

.swatch {
  width: 28px;
  height: 28px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: transform 0.12s;
  position: relative;
}

.swatch:active {
  transform: scale(0.92);
}

.swatch[data-active="true"] {
  box-shadow:
    0 0 0 2px var(--bg-secondary),
    0 0 0 4px currentColor;
}
```

- [ ] **Step 3: TypeScript 检查**

Run: `npx tsc --noEmit`
Expected: 通过

- [ ] **Step 4: 报告 diff**

Run: `git status src/components/ThemeSettings*`

---

## Task 7: 把 ThemeSettings 接进 Profile

**目标:** 在 Profile 页的"我的主题"section 上方插入 `<ThemeSettings/>`。

**Files:**
- Modify: `src/pages/Profile.tsx`

- [ ] **Step 1: import + 插入**

打开 `src/pages/Profile.tsx`:

在第 6 行(`import styles from './Profile.module.css'`)下面加:
```tsx
import ThemeSettings from '../components/ThemeSettings'
```

在 `<div className={styles.content}>` 内、"我的主题"那个 section(第 88-104 行)**上方**插入:
```tsx
<ThemeSettings />
```

具体定位:`<div className={styles.section}>` 包"我的主题"按钮的那段之前,加一行 `<ThemeSettings />`。如果 Profile 现在有 `bio` section 和 `links` section 在前面,`<ThemeSettings />` 放在它们之后、"我的主题"之前。

最终结构(content 内部从上到下):
1. bio section(如果有)
2. links section
3. **`<ThemeSettings />`**(新)
4. "我的主题"section
5. 退出登录 section
6. version

- [ ] **Step 2: TypeScript 检查**

Run: `npx tsc --noEmit`
Expected: 通过

- [ ] **Step 3: 端到端浏览器验证(全功能)**

后台启 `npm run dev`,chrome-devtools MCP:

1. 打开 `/profile`(如未登录先 cookie 注入或跳 login;若 dev server 没登录态,直接看其它页也能验主题切换 — 但 ThemeSettings UI 在 Profile,需要登录)
2. 若无法登录,把 dev server 改成临时 mock,或者直接在 console 跑 `document.location.href = '/profile'` 看是否有 ThemeSettings render — 如果 Profile 在未登录时跳走,可临时在 console 跑这段验 UI:
   ```js
   // 临时:绕过 useAuth gate
   ```
   实际上更简单:直接在 Home 页(无需登录)的 console 里 evaluate component? 不行。**实用做法**:dev 环境登录一下(passcode 在 `/tmp/v2fun-srv.log` 取,先 `node server/index.mjs`)
3. 进 Profile 页,看到"外观"section,有 mode toggle 和 6 个色块,当前选中 teal(外环高亮 + 中心 ✓)
4. 点 coral 色块:截图,全页 accent 变珊瑚橘,选中态从 teal 移到 coral
5. 点 "明亮" 按钮:截图,全页变浅色,accent 仍 coral
6. 刷新页面:截图,落到 light + coral(localStorage 持久化生效,无 FOUC)
7. console 跑 `JSON.stringify({theme: localStorage.getItem('v2fun_theme'), accent: localStorage.getItem('v2fun_accent')})`:Expected `{"theme":"light","accent":"coral"}`
8. 切回 dark + teal,刷新确认能回去

杀 dev server 和 backend server。

- [ ] **Step 4: 报告 diff**

Run: `git diff src/pages/Profile.tsx`

---

## Task 8: LAN dev server 自测(部署前必做)

**目标:** 按 CLAUDE.md 的"services 必须在 192.168.1.5 自测"规则,先在 LAN dev 验一遍。但 v2fun 不在 LAN dev 部署,**它直接在 prod 部署**;此项目无 LAN dev 部署,可跳过本 task,直接进 Task 9 commit + prod 部署。

- [ ] **Step 1: 跳过(本项目无 LAN dev 部署)**

记录:v2fun 直接走 GHCR build → prod pull,无 LAN 中转。Task 7 的本地 dev server 浏览器测试就是上线前的全功能验证。

---

## Task 9: Commit + Push + Prod 部署

**目标:** 请示用户后 commit 全部改动,push 触发 CI,CI 完成后 SSH 进 prod 跑 `bash v2fun.sh update`。

- [ ] **Step 1: 请示用户 commit**

汇报本次改动文件清单(新增 3 个 / 修改 6 个),问用户:

> "本地全部改完且 typecheck/browser 自测通过,要 commit 推 prod 吗?spec 和 plan 这两个 docs 文件要不要一起 commit?"

等用户明确"可以"或类似回复。

- [ ] **Step 2: Commit**

Run(用户同意后):
```bash
cd /Users/jiangxuan/Projects/v2fun
git add src/index.css src/main.tsx src/hooks/useTheme.tsx \
        src/components/ThemeSettings.tsx src/components/ThemeSettings.module.css \
        src/components/Layout.module.css src/components/PullToRefreshIndicator.module.css \
        src/pages/Login.module.css src/pages/Profile.tsx src/pages/Profile.module.css \
        index.html
# 如用户同意一起带 docs:
# git add docs/superpowers/specs/2026-05-20-theming-design.md docs/superpowers/plans/2026-05-20-theming.md
git commit -m "$(cat <<'EOF'
feat: add light/dark mode toggle and accent color palette

Switch default accent from cyan to softer teal (#5FA8A0).
Theme state persists in localStorage; inline script in index.html
prevents FOUC on reload. Settings UI lives in Profile under "外观".

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 3: Push**

Run: `git push origin main`
Expected: 推到远端,触发 CI(docker build)

- [ ] **Step 4: 等 CI**

Run: `gh run list --workflow=docker.yml -L 1`
然后 `gh run watch <run-id>` 或循环 `gh run view <run-id>` 等 success(预计 ~1m)。

- [ ] **Step 5: SSH 部署**

```bash
ssh jiangxuan@108.62.161.15 'cd /home/jiangxuan/dockers/v2fun && bash v2fun.sh update'
```

Expected: 拉新 image,容器重启,`docker ps | grep v2fun` 显示 Up <几秒>。

- [ ] **Step 6: 公网验证**

Run:
```bash
curl -sI https://v2fun.ohminicat.com | head -3
ssh jiangxuan@108.62.161.15 "docker ps --filter name=v2fun --format '{{.Image}} {{.Status}}'"
```

Expected: 200 + 容器 Up;image SHA 应是新的。

进一步:用 chrome-devtools MCP 打 `https://v2fun.ohminicat.com`,验证主题色默认是雾松绿、Profile 页有"外观"section。

- [ ] **Step 7: 收尾**

汇报上线状态,结束。

---

## Self-Review

- [x] **Spec coverage:** Token 三层 (Task 1) / 防 FOUC (Task 2) / ThemeProvider (Task 3-4) / 硬编码迁移 (Task 5) / Settings UI (Task 6-7) / 部署 (Task 9) 全覆盖。spec 的"不做"清单(跟随系统/动画/拾色器/服务端同步)在 plan 里也无人提及,符合 YAGNI。
- [x] **Placeholder scan:** 无 TBD/TODO。所有代码块完整。Task 7 step 3 的浏览器验证里有一小段"如无法登录,使用临时绕过"的描述,但给出了备用路径(用 dev server backend 登录)— 足够具体。
- [x] **Type consistency:** `ThemeMode = 'dark'|'light'`、`AccentSlug = 6 个具体 slug`、`ACCENTS` 数组结构 `{slug,name,color}`,在 useTheme.tsx / ThemeSettings.tsx 全程一致。`setTheme/setAccent` 签名在 Task 3 定义,Task 6 消费一致。
